import LoginForm from '../../components/login-form';

export default function LoginPage() {
    return (
        <main style={{ padding: '2rem' }}>
            <h1>Sign In</h1>
            <LoginForm />
        </main>
    );
}
